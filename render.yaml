services:
  - type: web
    name: brandguard-os
    env: static
    buildCommand: cd brand-guard-os && npm install && npm run build
    staticPublishPath: brand-guard-os/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
